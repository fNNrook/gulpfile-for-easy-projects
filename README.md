# gulpfile-for-easy-projects
An easy gulpfile, for little projects and doings

## installation:
`npm i` for install the dependencies



Use `npm run build` to build your Production Folder

Use `npm run start` to start your watch task 
